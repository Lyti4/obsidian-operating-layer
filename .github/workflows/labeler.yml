name: labeler

on:
  pull_request_target:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  labeler:
    name: label pull request
    runs-on: ubuntu-latest
    steps:
      - name: Label PR
        uses: actions/labeler@v5
