# Реестр инструментов

**Статус:** `active-migration`

Этот файл является единственным полным реестром `tools/*.py`. Каркас создан в
первом срезе дерева инструкций; строки 58 исходных инструментов добавляются в
User Story 2 функции `specs/001-instruction-tree-tool-documentation/`.

Пока покрытие не достигло динамического набора Git, этот файл нельзя считать
доказательством полного описания инструментов. Контракт таблицы находится в
`specs/001-instruction-tree-tool-documentation/contracts/tool-registry.md`.

## Покрытие

- Исходный набор: 58 отслеживаемых `tools/*.py` на коммите `2c9e432`.
- Текущее документированное покрытие: 0 из 58.
- Внутренние модули, которые будут отмечены отдельно: `tools/_bootstrap.py` и
  `tools/common.py`.

## Таблица

| tool | purpose | kind | family | mode | write_surface | inputs | outputs | test | instruction | spec | status |
|---|---|---|---|---|---|---|---|---|---|---|---|

## Правило изменения

Новый или изменённый инструмент одновременно получает строку таблицы, тест и
одно основное семейное руководство. `approved-write` без rollback-bearing
runbook (инструкции с откатом) блокирует завершение задачи.
